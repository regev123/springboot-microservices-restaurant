# Application name
spring.application.name=api_gateway_service

# JWT secret for tests (can be same as dev or a simpler one)
jwt.secret=Fj93jsLk2mN7XyZ9wT1qU0oPrsVx5aKb

# Whitelisted endpoints that bypass authentication
jwt.whitelist=/api/auth/login,/api/auth/register,/api/auth/changePassword

# Run API Gateway on default test port (inside container)
server.port=8081

# Docker Compose environment
auth.service.url=http://auth-service:8082